#pragma once
#include <iostream>
#include <string>

using namespace std;

bool isInt(char c);
bool isInt(string s);
bool checkDate(string date);
int inputInt(int minInput, int maxInput);
string inputDate();
